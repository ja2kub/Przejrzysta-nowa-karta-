
var handles = document.querySelectorAll(".resize-handle");
console.log("Handles count:", handles.length);
var clock = document.getElementById("clock");
console.log("Clock classes:", clock.className);
console.log("Clock innerHTML handles:", clock.querySelectorAll(".resize-handle").length);
